export class User  {
  constructor (firstName, lastName, email, password, contactNumber) {
    this.firstName = firstName;
    this.lastName = lastName;
    this.email = email;
    this.password = password;
    this.contactNumber = contactNumber;
  }
}
